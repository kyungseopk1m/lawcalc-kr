/**
 * ISO date string in the form `YYYY-MM-DD`. Defined locally to keep this
 * package dependency-free; structurally identical to `IsoDate` in
 * `@lawcalc-kr/core-engine` (both are unbranded `string`).
 */
export type IsoDate = string;
