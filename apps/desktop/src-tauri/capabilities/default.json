{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "LawCalc Korea 기본 권한 — 사용자 선택 경로 dialog만 허용. fs 직접 접근은 W4에서 평가",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "dialog:allow-save"
  ]
}
